/**
 * Stub getBookerBaseUrlSync for open-source version
 */
export function getBookerBaseUrlSync(_teamOrOrgSlugOrId: string | number | null) {
  // In open-source, org domains are simplified
  // Accept either slug (string), orgId (number), or null for compatibility
  return "";
}
