/**
 * Stub payments webhook for open-source version
 */
const handleWebhook = async (_args: any): Promise<void> => {
  // No-op for OSS - payments features disabled
};

export default handleWebhook;
