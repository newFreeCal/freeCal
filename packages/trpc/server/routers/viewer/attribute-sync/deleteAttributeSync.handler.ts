type DeleteAttributeSyncOptions = {
  ctx: {
    user: {
      id: number;
    };
  };
  input: any;
};

const deleteAttributeSyncHandler = async ({}: DeleteAttributeSyncOptions) => {
  return null;
};

export default deleteAttributeSyncHandler;
