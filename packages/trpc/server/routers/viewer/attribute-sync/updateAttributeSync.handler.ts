type UpdateAttributeSyncOptions = {
  ctx: {
    user: {
      id: number;
    };
  };
  input: any;
};

const updateAttributeSyncHandler = async ({}: UpdateAttributeSyncOptions) => {
  return null;
};

export default updateAttributeSyncHandler;
