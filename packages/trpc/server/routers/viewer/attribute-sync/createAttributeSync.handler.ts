type CreateAttributeSyncOptions = {
  ctx: {
    user: {
      id: number;
    };
  };
  input: any;
};

const createAttributeSyncHandler = async ({}: CreateAttributeSyncOptions) => {
  return null;
};

export default createAttributeSyncHandler;
