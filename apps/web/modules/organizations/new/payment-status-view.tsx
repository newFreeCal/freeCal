/**
 * Stub for open-source version
 * Replaces EE-only payment status view
 */
export default function PaymentStatusView() {
  return <></>;
}

export const LayoutWrapper = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};
