/**
 * Stub for open-source version
 * Replaces EE-only features view
 */
import type { ReactElement } from "react";

export type OrganizationFeaturesViewProps = {
  canEdit: boolean;
};

export default function OrganizationFeaturesView({ canEdit }: OrganizationFeaturesViewProps): ReactElement {
  return <></>;
}
