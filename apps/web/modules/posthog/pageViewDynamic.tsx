/**
 * Stub for open-source version
 * Replaces EE-only PostHog page view
 */
import type { ReactElement } from "react";

export default function DynamicPostHogPageView(): ReactElement {
  return <></>;
}
